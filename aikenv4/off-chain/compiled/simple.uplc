# the CBOR HEX for the smart contract transaction will go here it will be created when you use the command: ....
# ...aiken build --output simple.uplc after the locking script is created and the validator is compiled